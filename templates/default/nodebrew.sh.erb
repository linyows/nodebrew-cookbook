# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overridden

if [ -s "${HOME}/.nodebrew/bin" ]; then
  nodebrew_root="${HOME}/.nodebrew"
elif [ -s "<%= @nodebrew_root %>" ]; then
  nodebrew_root="<%= @nodebrew_root %>"
  export NODEBREW_ROOT="$nodebrew_root"
fi

if [ -n "$nodebrew_root" ]; then
  export PATH="${nodebrew_root}/current/bin:$PATH"
fi
